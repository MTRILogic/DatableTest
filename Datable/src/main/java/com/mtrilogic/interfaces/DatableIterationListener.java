package com.mtrilogic.interfaces;

import com.mtrilogic.abstracts.Model;
import com.mtrilogic.classes.Datable;

public interface DatableIterationListener {

    Model onDatableIteration(Datable datable);
}
